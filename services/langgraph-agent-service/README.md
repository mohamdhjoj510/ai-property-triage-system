# LangGraph Agent Service

Orchestration layer built with LangGraph. Decides which downstream service to call (RAG, image analyser, guardrails) and in what order to produce a final triage decision.

Placeholder — implementation to follow.
